
setTimeout(() => {
    const msg = document.getElementById("msgSuccess");
    if(msg) msg.remove();
}, 3000);
setTimeout(() => {
    const msg = document.getElementById("msgError");
    if(msg) msg.remove();
}, 3000);
